# CRA reads this at build time. For Vercel Production, set in Project → Environment Variables.

# Public URL of the FastAPI backend (no trailing slash), e.g. https://chutti-api.vercel.app
REACT_APP_BACKEND_URL=http://localhost:8000
